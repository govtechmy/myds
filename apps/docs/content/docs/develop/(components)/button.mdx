---
title: Button
description: A fundamental UI element used to trigger actions or events. It ensures consistency, accessibility, and a clear call to action.
---

import {
  InfoIcon,
  ArrowForwardIcon,
  ArrowBackIcon,
  UploadIcon,
  CopyIcon,
  TrashIcon,
} from "@govtechmy/myds-react/icon";
import { Quicklinks } from "@/components/quicklinks";
import Preview from "@/components/preview";
import {
  PreviewButton,
  Button,
  ButtonIcon,
  ButtonCounter,
  Link,
} from "@/components/myds";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
  <div className="flex justify-center">
      <PreviewButton
        variant="primary-fill"
        size="small"
        pantun="Berjalan-jalan ke kota tua, Membeli kuih di pasar pagi. Membuka jalan menuju dunia, 'Button' ditekan dengan jari. -- Copilot"
      >
        <span className="text-white">Jana Pantun</span>
      </PreviewButton>
      </div>
  </Tab>
  <Tab value="Code">
  ```tsx 
import { Button } from "@govtechmy/myds-react/button";

<Button variant="primary-fill" size="small" onClick={...}>
  Jana Pantun
</Button>;
```
  </Tab>
</Tabs>

<Quicklinks
  links={{
    storybook:
      "https://myds-storybook.vercel.app/?path=%2Fdocs%2Fgovtechmy-myds-react-button--docs",
    source:
      "https://github.com/govtechmy/myds/blob/main/packages/react/src/components/button.tsx",
  }}
/>

## Usage

```tsx copy
import {
  Button,
  ButtonIcon,
  ButtonCounter,
} from "@govtechmy/myds-react/button";
```

```tsx copy
export default () => (
  <Button>
    <ButtonIcon />
    <ButtonCounter />
  </Button>
);
```

## Examples

### Variant

Use the `variant` prop to change the button style!

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
  <div className="space-y-2">
  <div className="flex justify-center items-center h-fit gap-1">
      <Button
        variant="primary-fill"
        size="small"
      >
        <span className="text-white">Primary Fill</span>
      </Button>

      <Button
        variant="primary-outline"
        size="small"
      >
        <span>Primary Outline</span>
      </Button>

      <Button
        variant="primary-ghost"
        size="small"
      >
        <span>Primary Ghost</span>
      </Button>
      </div>

  <div className="flex justify-center items-center h-fit gap-1">
      <Button
        variant="danger-fill"
        size="small"
    
      >
        <span className="text-white">Danger Fill</span>
      </Button>

      <Button
        variant="danger-outline"
        size="small"
      >
        <span>Danger Outline</span>
      </Button>

      <Button
        variant="danger-ghost"
        size="small"
      >
        <span>Danger Ghost</span>
      </Button>
      </div>

  <div className="flex justify-center items-center h-fit gap-1">
      <Button
        variant="default-outline"
        size="small"
      >
        <span>Default Outline</span>
      </Button>

      <Button
        variant="default-ghost"
        size="small"
      >
        <span>Default Ghost</span>
      </Button>
      </div>

  </div>
  </Tab>
  <Tab value="Code">
  ```tsx 
import { Button } from "@govtechmy/myds-react/button";

/** Varian: Primary **/

<Button variant="primary-fill">Primary Fill</Button>;
<Button variant="primary-outline">Primary Outline</Button>;
<Button variant="primary-ghost">Primary Ghost</Button>;

/** Varian: Danger **/

<Button variant="danger-fill">Danger Fill</Button>;
<Button variant="danger-outline">Danger Outline</Button>;
<Button variant="danger-ghost">Danger Ghost</Button>;

/** Varian: Default **/

<Button variant="default-outline">Default Outline</Button>;
<Button variant="default-ghost">Default Ghost</Button>;
```
  </Tab>
</Tabs>

### Size

Use the `size` prop to change the button size!

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
  <div className="space-y-2">
  <div className="flex justify-center items-center h-fit gap-1">
      <Button size="small" variant="default-outline">
        <span>small</span>
      </Button>

      <Button
        size="medium"
        variant="default-outline"
      >
        <span>medium</span>
      </Button>

      <Button
        variant="default-outline"
        size="large"
      >
        <span>large</span>
      </Button>
      </div>

  </div>
  </Tab>
  <Tab value="Code">
  ```tsx 
import { Button } from "@govtechmy/myds-react/button";

/** Size **/

<Button size="small" variant="default-outline">small</Button>;
<Button size="medium" variant="default-outline">medium</Button>;
<Button size="large" variant="default-outline">large</Button>;
```
  </Tab>
</Tabs>

### With Icon

Use `<ButtonIcon>` to add action icon to the button.

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
  <div className="space-y-2">
  <div className="flex justify-center items-center h-fit gap-1">
     <Button variant="default-outline" size="medium">
      <ButtonIcon>
        <ArrowBackIcon />
      </ButtonIcon>
      <span>Go back</span>
    </Button>
     
    <Button variant="default-outline" size="medium">
      <span>Go forward</span>
      <ButtonIcon>
        <ArrowForwardIcon />
      </ButtonIcon>
    </Button>

      </div>

  </div>
  </Tab>
  <Tab value="Code">
```tsx 
import { Button, ButtonIcon } from "@govtechmy/myds-react/button";
import {
  ArrowForwardIcon,
  ArrowBackIcon,
} from "@govtechmy/myds-react/icon";

/** With Icon; Position: Start **/

<Button variant="default-outline" size="medium">
  <ButtonIcon>
    <ArrowBackIcon />
  </ButtonIcon>
  <span>Go back</span>
</Button>

/** With Icon; Position: End **/

<Button variant="default-outline" size="medium">
  <span>Go forward</span>
  <ButtonIcon>
    <ArrowForwardIcon />
  </ButtonIcon>
</Button>
```
  </Tab>
</Tabs>

### Icon Only

For buttons with sole icon - set `iconOnly=true` & use `<ButtonIcon>` as the only element in the `children`.

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
  <div className="flex justify-center items-center h-fit gap-1">
    <Button variant="primary-fill" size="small" iconOnly>
      <ButtonIcon>
        <UploadIcon />
      </ButtonIcon>
    </Button>

    <Button variant="default-outline" size="small" iconOnly>
      <ButtonIcon>
        <CopyIcon />
      </ButtonIcon>
    </Button>

    <Button variant="danger-fill" size="small" iconOnly>
      <ButtonIcon>
        <TrashIcon />
      </ButtonIcon>
    </Button>

      </div>

  </Tab>
  <Tab value="Code">
```tsx 
import { Button, ButtonIcon } from "@govtechmy/myds-react/button";
import {
  UploadIcon,
  CopyIcon,
  TrashIcon,
} from "@govtechmy/myds-react/icon";

<Button variant="primary-fill" size="small" iconOnly>
  <ButtonIcon>
    <UploadIcon />
  </ButtonIcon>
</Button>

<Button variant="default-outline" size="small" iconOnly>
  <ButtonIcon>
    <CopyIcon />
  </ButtonIcon>
</Button>

<Button variant="danger-fill" size="small" iconOnly>
  <ButtonIcon>
    <TrashIcon />
  </ButtonIcon>
</Button>
```
  </Tab>
</Tabs>

### With Counter

Use `<ButtonCounter>` to add numeric information to the button.

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
  <div className="space-y-2">
  <div className="flex justify-center items-center h-fit gap-1">
     <Button variant="primary-fill" size="medium">
      <span className="text-white">Filter</span>
      <ButtonCounter>
        5
      </ButtonCounter>
    </Button>
    <Button variant="default-outline" size="medium">
      <span>Filter</span>
      <ButtonCounter>
        <span className="text-white">5</span>
      </ButtonCounter>
    </Button>

    <Button variant="danger-fill" size="medium">
      <span className="text-white">Filter</span>
      <ButtonCounter>
        <span>5</span>
      </ButtonCounter>
    </Button>

  </div>

  </div>
  </Tab>
  <Tab value="Code">
```tsx
import { Button, ButtonCounter } from "@govtechmy/myds-react/button";

<Button variant="primary-fill" size="medium">
  Filter
  <ButtonCounter>5</ButtonCounter>
</Button>

<Button variant="default-outline" size="medium">
  Filter
  <ButtonCounter>5</ButtonCounter>
</Button>

<Button variant="danger-fill" size="medium">
  Filter
  <ButtonCounter>5</ButtonCounter>
</Button>
```

  </Tab>
</Tabs>

## Props

### Button

<TypeTable
  type={{
    variant: {
      description: "Style variant of the button",
      type: "enum",
      typeDescription:
        "primary-fill | primary-outline | primary-ghost | danger-fill | danger-outline | danger-ghost | default-outline | default-ghost",
      default: "default-outline",
    },
    size: {
      description: "Sizes of the button",
      type: "enum",
      typeDescription: "small | medium | large",
      default: "small",
    },
    iconOnly: {
      description: "Button with sole icon",
      type: "boolean",
      default: "false",
    },
    children: {
      description: "Content of the button",
      type: "ReactNode",
    },
    className: {
      description: "Additional styles",
      type: "string",
    },
  }}
/>

### ButtonIcon

<TypeTable
  type={{
    children: {
      description: "Import any icon component from @govtechmy/myds-react/icon",
      type: "ReactNode",
    },
    className: {
      description: "Additional styles",
      type: "string",
    },
  }}
/>

### ButtonCounter

<TypeTable
  type={{
    children: {
      description: "Numeric information to be displayed",
      type: "ReactNode",
    },
    className: {
      description: "Additional styles",
      type: "string",
    },
  }}
/>
