---
title: Link
description: This component extends the <a> HTML element (or anchor element) and is used to display hyperlinks.
---

import { Quicklinks } from "@/components/quicklinks";
import Preview from "@/components/preview";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Link } from "@/components/myds";
import NextLink from "next/link";
import { InfoIcon } from "@govtechmy/myds-react/icon";

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
    <div className="justify-center relative not-prose">
      <Link href="/" primary underline="always">
        {/* Text-inherit for span to overwrite Fumadocs text color */}
        <span className="text-inherit">Malaysia's Official Design System</span>
      </Link>
    </div>
  </Tab>
  <Tab value="Code">
    ```tsx 
    import { Link } from "@govtechmy/myds-react/link";

    <Link href="/" primary underline="always">
      {/* The link's destination should be indicated here. */}
      Malaysia's Official Design System
    </Link>
    ```

  </Tab>
</Tabs>

<Quicklinks
  links={{
    storybook:
      "https://myds-storybook.vercel.app/?path=%2Fdocs%2Fgovtechmy-myds-react-link--docs",
    source:
      "https://github.com/govtechmy/myds/blob/main/packages/react/src/components/link.tsx",
  }}
/>

## Usage

```ts copy title="Import"
import { Link } from "@govtechmy/myds-react/link";
```

```tsx copy title="Anatomy"
export default () => (
  <Link href="/" primary>
    {/* The link's destination should be indicated here. */}
    Malaysia's Official Design System
  </Link>
);
```

## Examples

### Using asChild for Custom Links

If you need to use the `Link` component provided by a different package, you can use the
`asChild` prop and wrap the component. Here's an example using Next.js:

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
    <Link asChild>
      <NextLink href="#using-aschild-for-custom-links">
        Custom Link
      </NextLink>
    </Link>
  </Tab>
  <Tab value="Code">
    ```tsx 
    import { Link } from "@govtechmy/myds-react/link";
    import NextLink from "next/link";

    <Link asChild>
      <NextLink href="#">Custom Link</NextLink>
    </Link>
    ```

  </Tab>
</Tabs>

You can also create a custom component and import it as needed:

```tsx
import { Link as LinkPrimitive } from "@govtechmy/myds-react/link";
import NextLink from "next/link";

export const Link = (props) => (
  <LinkPrimitive asChild>
    <NextLink {...props} />
  </LinkPrimitive>
);
```

### Open in a New Tab

Opens the link in a new tab by setting `target="_blank"`.

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
    <Link href="https://digital.gov.my" newTab>Ministry of Digital</Link>
  </Tab>
  <Tab value="Code">
    ```tsx 
    import { Link } from "@govtechmy/myds-react/link";

    <Link href="https://digital.gov.my" newTab>Ministry of Digital</Link>
    ```

  </Tab>
</Tabs>

<Callout
  type="info"
  title="Note"
  className="bg-bg-primary-50 text-txt-primary border-transparent"
  icon={<InfoIcon className="size-5" />}
>
  All current versions of major browsers now automatically use the behaviour of
  `rel="noopener"` for any links with `target="_blank"`
</Callout>

### Primary

Used to toggle the link text colour.

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
    <div className="space-y-2">
      <div className="flex justify-center items-center gap-2">
        <Link href="#primary" primary>Primary</Link>
        <Link href="#primary">Inherit from parent (default)</Link>
      </div>
    </div>
  </Tab>
  <Tab value="Code">
    ```tsx 
    import { Link } from "@govtechmy/myds-react/link";

    <Link href="#" primary>Primary</Link>
    <Link href="#">Inherit from parent</Link>
    ```

  </Tab>
</Tabs>

### Underline

Controls when the link should have an underline.

<Tabs items={["Preview", "Code"]} defaultValue="Preview">
  <Tab value="Preview">
    <div className="space-y-2">
      <div className="flex justify-center items-center h-fit gap-2 not-prose">
        <Link href="#underline" underline="always" >Always</Link>
        <Link href="#underline" underline="hover" >Hover</Link>
        <Link href="#underline" underline="none" >None</Link>
      </div>
    </div>
  </Tab>
  <Tab value="Code">
    ```tsx 
    import { Link } from "@govtechmy/myds-react/link";

    <Link href="#underline" underline="always">Always</Link>
    <Link href="#underline" underline="hover">Hover</Link>
    <Link href="#underline" underline="none">None</Link>
    ```

  </Tab>
</Tabs>

## Props

### Link

<TypeTable
  type={{
    asChild: {
      description:
        "Renders the child element instead of the default element, while preserving the component's props and functionality.",
      type: "boolean",
    },
    href: {
      description: "The URL that the hyperlink points to.",
      type: "string",
    },
    newTab: {
      description: "Controls whether the link opens in a new tab.",
      type: "boolean",
      default: "false",
    },
    primary: {
      description: "Sets the text colour to primary or inherit it from parent.",
      type: "boolean",
      default: "false",
    },
    underline: {
      description: "Controls when the link should have an underline.",
      type: "enum",
      typeDescription: "always | hover | none",
      default: "always",
    },
  }}
/>
